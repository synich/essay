# 大模型3推理过程与实现

## 推理流程的抽象与拆分

hugging face将所有的 LLM 抽象成了 text-generation pipeline，由 Tokenizer 和 Model 两部分组成。

tokenizer 需要继承PreTrainedTokenizer类进行实现，该类提供了apply_chat_template方法，将多轮对话的 JSON输入 转换为一个string。也就是经常看到的 `<|im_start|>` 开头的长长的文本。

llm模型的跨平台部署抽象为4部分

1. tokenizer: 处理自然语言输入的关键，将原始文本转换为模型能理解的格式
2. embedding: 向量长度和网络的hidden_size相等，在1k~4k不等。为了节约内存，引入了disk embedding
3. blocks/layer: 由于LLM的主干网络由一系列连续的block组成。它最主要的两个计算操作分别是Linear（线性化）和MatMul（矩阵乘），除此之外还伴随诸如split、concat和transpose等内存操作，统称为Memory算子
4. lm头: 将最后一个block的MLP输出的hidden_size维，转成vocab维，用于最终的token输出。这个矩阵的形状和最开始的embeding是转置关系，有些模型甚至权重都完全复用embedding

由于block数量很多，全部加载对于小内存设备会不够，ollama的优化策略是开始只加载一个block到内存中，在该block执行推理时，异步加载下一个block；等到加载完毕在执行推理时清空第一个block；这样依次执行，内存中最多有2个block存在，大大降低内存需求。

## 推理各环节

当输入字符串转换为向量空间的浮点数，进入GPU之后的推理可以分为两个阶段：prefill和decode。

在prefill阶段，输入一个包含m个token的提示（prompt），执行batch为m的推理，此时由于是初始输入，没有kv-cache的需求，得到了首个token的输出。接着这个token作为输入，开始decode阶段。每完成一次decode，kv-cache的长度相应+1。单个block的耗时分析

1. prefill阶段，Linear算子的耗时占比相对稳定，超过了93%，而MatMul和Memory算子的耗时占比分别约为3%和2%；这个阶段处理大量输入数据，使用矩阵乘法（GEMM），是计算密集过程，需要用SIMD优化
2. decode阶段，随着m+n的增长，Linear算子的时间占比有所下降，而MatMul和Memory算子的占比有所上升。尽管如此，在多数情况下，耗时主要集中在Linear算子上。这个阶段使用矩阵向量乘法（GEMV），访存的效率变得更加关键

### GPT2词表构建

一种防止表意歧义的编码机制，接受正常的UTF-8输入，把每个字节都表示为可见的unicode码点(伪Unicode，且使用尽量小的码点段)，最后再转成UTF-8并base64编码保存成tokenizer

8字节的latin-1字符集，可见字符范围`0x21~0x7E 0xA1~0xAC 0xAE~0xFF`。空出来的`0xAD`对应soft hyphen，是个条件可见字符，所以也归类到不可见。不可见字符根据它的序号再加上256成为可见

以空格`0x20`为例，属于第21(序号20)个不可见，所以加256变成`0x0120`，将它逐字节UTF-8编码，变成`C4 A0`两个字节

中文膨胀得更严重，比如"你"的UTF-8对应`E4 BD A0`，前2个可见不用转换，最后的A0属于第67个(序号66)不可见字符，66＋256转成0x0142。接下来再对 `E4 BD 0142` 分别进行UTF-8编码，最终变成 `C3 A4 C2 BD C5 82` 共6个字符，膨胀率比urlencode还是要少一点

### 公式推导

从MHA开始推导，仅推导1层attn和ffn，以下为模型核心形状

- hidden_size: 1024
- head_num: QKV全为8，即head_size=128
- vocab: 120K
- FFN升维按4倍算

embedding和lm的形状是[1024, 120K]

### prefill

1. 假设输入50token，查表后取出每个token的语义向量，得到输入矩阵[50, 1024]
2. 按head拆开，并将头的个数调整到最开始，transpose得到[8, 50, 128]
3. QKV的形状是[128, 128]，投影后W_Q/W_K/W_V都是[50, 128]
4. 多头拼接后得到矩阵[8, 50, 128]，reshape得到[50, 1024]
5. 归一化，不改变形状
6. FFN升维再降维，先变成[50, 4096]再变回[50, 1024]

### decode

prefill始终在语义空间计算，不涉及输出，所以不体现vocab维，到decode就要用到vocab了

1. 将上一步的[50, 1024]和lm的[1024, 120K]相乘，得到[50, 120K]，取最后一个输出[120K]的向量，查表转成人类可读的文字

最后一步输出的[120K]向量又叫logits，会从NPU向CPU复制，每个元素是BF16类型，大约300K内存拷贝，是一笔不小的开销。

内存就绪后轮到CPU计算，如果是贪心算法比较简单，10万个数组做一次遍历取出最大值；如果是top-p就需要先遍历并做一次温度除尘，然后再遍历求exp，接着还要排序并找出满足p的若干个候选值，计算量很大。如果是服务器CPU速度比较快，对嵌入式ARM处理器，一次采样要20ms左右，会极大拖慢decode速度。


## 推理引擎

主要优化点是提升计算速度和优化硬件资源，包括量化、内存/显存的使用

* olamma: 适合个人使用，集模型联网管理+lamma.cpp推理引擎于一身
* vLLM/SGLang: 支持多并发，一般企业使用

```
export OLLAMA_HOST=0.0.0.0:11434
ollama serve&
```

### MindIE

主要由service/llm(或sd)/rt三大核心部分构成

* service: 负责处理http请求并拉起llm模型，但不是简单的http server，有很多策略管理的细节。用了很多GRPC的库
* llm/sd: 负责将模型的checkpoint文件加载并做多层的合并和图编译，这块是用python和torch的api写的。sd为多模态版本
* rt: 创建和管理计算上下文context/stream，将优化后的计算指令下发给NPU(launch kernel)，硬件加速依赖atb/nnal库

Attention依赖cube单元，而FFN和Norm则依赖vector单元。

## 采样的若干概念

大模型推理会生成每个token的概率分布，最终输出哪个token通过以下参数调节效果

确定性策略（相比其它概念，这个属于确定类）

* Greedy Search: chat模式几乎都是这个策略，适合流式输出
* Beam Search: 对几个路径进行概率总和评估后，选择最高的输出，适合允许等待且准确性高的场景
* Diverse Beam Search

logits采样

* Temperature/Top-P(Nucleus): 核抽样算法，温度越高top-p越接近1，取值越随机；反之越确定。当温度为0或top-p极小比如0.01，退化为贪心。
* Top-K抽样: 和温度无关，固定采样排名前K个候选值。比较拍脑袋，我自己很少使用
* Rejection 拒绝抽样

惩罚

* Repetition 重复惩罚
* Length 长度惩罚

解码

* Chunk-wise 分块解码
* Staged 分阶段解码
* Contrastive 对比解码
* RL 基于强化学习解码
