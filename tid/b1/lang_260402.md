# K和forth语言

## 与J的比较

* J 和 K 同样基于向量, 但语言设计上差异很大
* J 有专门的 parser 处理 7 种词性(名词动词连词), 而 K 只有 3 种, 勉强对应名词-变量/动词-函数/adverb 副词(只有'/\或加上:的双副词)
* J 和 APL 都有 train(2 train/3 train), 但 K 要求显式定义函数
* J 规定凡是函数必须 dyadic(因为要支持 train), K 则规定如果定义只有 x, 就只能传一个参数;反之定义有 xy 则只能传二个参数
* J 有 rank/cell/frame, 要求维度匹配, 而 k 似乎是原子穿透, 可以用'each 改变函数的作用行为

## kona(k3)

-x选项虽然默认打印，但结果和直觉不符合。`k -x '"hi\n"'`打印出`"hi\n"`，带双引号，同时也不转义。但 <code>k -e '`0: "hi\n"'</code> 就打印出hi并带回车，符合直觉

函数在左，参数在右；从右向左依次执行，沿用数学的`f(g(h(x)))`表示法。

string和symbol差异极大

* `@` 判断原子性，string返回0（不是），symbol返回1
* `0:` 只支持symbol，不支持string
* <code>\`0:</code> 只支持string，要用`$`转换，比如`$1`实现str(1)的效果。反过来写成`0$"1"`则是int("1")。猜测打印是向char设备流式发送字符，symbol作为内部表示不能被拆开，所以要先转成string才能打印
* symbol不能数字开头，会报错parser error

k 的赋值:是 amend(.[x;y;f;z])的语法糖. 而且对 x 位, a 返回新值, 但`a 会原地修改

### 读csv

差异过于巨大，颠覆！读一个3列的文件，文档说支持各种类型，实测只能是C，转换为数字时只支持同一类型：

<code>k -x 'd: ("CCC"; ",") 0: `a.csv;v: {{0$x} each x} each d'</code>

这里的each可选，没有也不影响结果，缺点是要求列类型必须相同。下面列表构造表达式写法，支持不同列分开解析

<code>k -x 'd: ("CCC"; ",") 0: `a.csv;v: ((0$d 0); (0$d 1); (0.0$d 2))'</code>

* `v 0` 和 `#v` 是列维度，所以`v 0`是第一列，`#v`是3
* `+/ v` 和 `+/ each v`都是取列的相同位置元素求和；`+/' v`是对每列各自求和

## forth

双栈+词典构建语法核心
